<% @posts.each do |t| %>
    <div class="post">
      <%= image_tag t.image_url, size: "250x200" if t.image? %>
      <%= t.user.email %>
      <%= t.name %>
      <%= t.area %>
      <%= t.price %>
      <%= t.number %>
    </div>
<% end %>
